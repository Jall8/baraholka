
  # BetBrawl app

  This is a code bundle for BetBrawl app. The original project is available at https://www.figma.com/design/qPxlAmI9xOLlsPQsSzcrvZ/BetBrawl-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  